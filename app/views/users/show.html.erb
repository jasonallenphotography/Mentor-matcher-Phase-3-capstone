<h1 class="text-center"><b><%= @user.first_name %> <%= @user.last_name %></b> |
  <% if @user.status == 'available' %>
    <% if @user.type == 'Mentor' %>
      Offering
    <% else %>
      Seeking
    <% end %>
  <% end %></h1>

<!-- If errors - place here -->

  <div class="row">
    <div class="small-4 large-4 columns">
      <%=image_tag current_user.picture_url %>
    </div>
    <div class="small-8 columns">
      <br>
      <small><%= current_user.current_title %> at</small><br>
      <b><%= current_user.current_company%></b><br>
    </div>
  </div>

<hr class="style-two">

  <div class="row">
    <div class="small-12 small-centered columns text-center">
      <i><%= @user.mission_statement %></i>
    </div>
    <!-- <i class="step fi-pencil size-24"></i> -->
  </div>
    <% if @user.id != current_user.id %>
      <form action="#" method="post">
        <input type="hidden" method="_update">
        <input class="button" type="button" value='Interested?'>
      </form>
    <%end%>

<hr class="style-three">


<!-- <% interests = nil %>
<% if @user.interests == nil %> -->
  <p>You don't have any interests yet! Add some here.</p>
  <!-- <a href="/users/<%=@user.id%>/finish" class="button round">Add Interests</a> -->
  <!-- <form action="/users/finish" method="get">
    <input class="button" type="button" value="Add interests">
  </form>
<% else %> -->

<!-- #make each interest clickable and look like a button taht you can see other users with this interest -->
  <% if @user.interests %>
    <div class="row">
      <p class="text-center">Current interests</p>
        <% @user.interests.each do |interest| %>
        <div class="small-6 columns">
          <a href="/users" class="button expanded"><%= interest.name %></a>
        </div>
      <% end %>
    </div>
  <% end %>
<!-- <a href="/auth/linkedin" class="button round">Sign up through LinkedIn</a> -->

  <!-- <form action="#" method="get">
    <input class="button" type="button" value="Edit interests">
  </form>
<% end %></br> -->

<div class="row">
  <div class="small-12 columns text-center">
    <%= form_for(:user_interests, url: user_interests_path, html: {method: "create"}) do |f| %>
      <table>
        <tr>
          <!-- <td>
            Add some interests here!
          </td> -->

          <td>
            <select name="interest_id" multiple>
              <% @interests.each do |int| %>
                <option value="<%=int.id%>"><%=int.name%></option>
              <%end%>
            <select>
          </td>
        </tr>
        <tr>
          <td>
            <%= f.submit value="Add Interest!" %>
          </td>
        </tr>
      </table>
    <% end %>
  </div>
</div>

<hr class="style-three">

  <% if @user.id != current_user.id %>
    <a href="<%= @user.public_profile_url %>" class="button">View me on LinkedIn</a>
  <% else %>
    <div class="button-group text-center">
      <a href="<%= @user.public_profile_url %>" class="button">View me on LinkedIn</a>
      <a href="https://www.linkedin.com/profile/edit?trk=nav_responsive_sub_nav_edit_profile" target='_empty' class="button">Edit my LinkedIn Profile</a>
    </div>
  <%end%>

<hr class="style-three">

