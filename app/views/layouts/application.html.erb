<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <nav>
        <div class="title-bar" data-responsive-toggle="main-menu" data-hide-for="large">
          <button class="menu-icon" type="button" data-toggle></button>
          <div class="title-bar-title"><a href="/">Mentorship</a></div>
        </div>
        <div class="top-bar" id="main-menu">
          <div class="top-bar-left">
            <ul class="vertical dropdown menu" data-dropdown-menu>
              <% if logged_in? %>
                <li><a href="/matches">My <%= current_user.type == "Mentor" ? "Seeker" : "Mentor"%> Requests</a></li>
                <li><a href="/users/<%=current_user.id%>"><i class="step fi-mail size-24"></i> My Inbox</a></li>
                <li><a href="/users/<%=current_user.id%>">My Profile</a></li>
                <li><a href="/users/<%=current_user.id%>">My Profile</a></li>
              <% else %>
                <li><a href="/auth/linkedin">Login</a></li>
                <li><a href="/auth/linkedin">Register</a></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

  <%= yield %>

<% if logged_in? %>
  <br>
  <div class="small-12 small-centered columns text-center">
    <footer>
      <p class='text-center'>You are logged in as <%= current_user.first_name %> <%= current_user.last_name %></p>
        <div class="expanded stacked-for-small button-group">
          <a href="/users/<%=current_user.id%>/edit" class="button success">Edit Profile</a>
          <a href="/logout" class="button alert">Logout</a>
        </div>
    </footer>
  </div>

<% end %>
  </body>

</html>
